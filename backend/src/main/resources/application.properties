spring.application.name=backend


# --- MySQL Configuration (Active) ---
spring.datasource.url=jdbc:mysql://localhost:3306/jobportaldb?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=manvith1
spring.jpa.hibernate.ddl-auto=update
# Note: Spring Boot 3+ automatically detects the MySQL dialect, so we don't strictly need to define it.

spring.jpa.show-sql=true
server.port=8080
